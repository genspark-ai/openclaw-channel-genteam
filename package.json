{
  "name": "@genspark/genteam",
  "version": "0.3.2",
  "description": "OpenClaw channel plugin connecting a self-hosted OpenClaw gateway to GenTeam as an Agent runtime",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist/**",
    "openclaw.plugin.json",
    "README.md"
  ],
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=node --external:ws --external:openclaw --outfile=dist/index.js",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "ws": "^8.21.0"
  },
  "peerDependencies": {
    "openclaw": ">=2026.4.23"
  },
  "peerDependenciesMeta": {
    "openclaw": {
      "optional": true
    }
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "channel": {
      "id": "genteam",
      "label": "GenTeam",
      "selectionLabel": "GenTeam",
      "docsPath": "/channels/genteam",
      "docsLabel": "genteam",
      "blurb": "Connect this gateway to GenTeam as an Agent runtime.",
      "order": 70
    },
    "compat": {
      "pluginApi": ">=2026.4.23"
    },
    "build": {
      "openclawVersion": "2026.4.23"
    }
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.28.1",
    "tsx": "^4.22.4",
    "typebox": "^1.3.0",
    "typescript": "^5.9.3"
  }
}
